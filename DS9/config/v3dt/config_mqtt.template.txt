# Credential-free local MQTT adapter template for the isolated MV3DT evaluation lane.
# Runtime materializes an owner-only copy outside the checkout. The canonical
# deferred profile remains non-activatable without the explicit evaluation gate.
[message-broker]
set-threaded = 0
