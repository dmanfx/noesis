# MQTT protocol adaptor settings for MV3DT communicator.
# Broker address/ports and topics are defined in config/v3dt/pub_sub_info_config_0.yml
# (localhost:1883 with ds3d/* topic prefix).
[message-broker]
username = user
password = password
client-id = noesis-mv3dt
enable-tls = 0
#tls-cafile =
#tls-capath =
#tls-certfile =
#tls-keyfile =
#share-connection = 1
#loop-timeout = 2000
#keep-alive = 60
#set-threaded = 1
