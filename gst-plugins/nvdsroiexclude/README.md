# Archived DS8 ROI-exclusion source

This root copy is retained only for source history. Do not build or install it.
The canonical native DS9.1 source/build/runtime contract is under
`DS9/csrc/nvdsroiexclude/` and `DS9/docs/Static_ROI_Exclusion.md`.
