[property]
# nvdsosd configuration for DS8 OSD rendering.
# process-mode: 0=CPU, 1=GPU (DeepStream nvdsosd)
process-mode=1

# Toggle annotation types
display-text=1
display-bbox=1
display-mask=1

# Note: font size/color are controlled per-object via NvDsObjectMeta.text_params.
# Adjusted in runtime hook code for larger, readable labels.
