[property]
# nvdsosd configuration used by deepstream_video_pipeline.py
# process-mode: 0=CPU, 1=GPU (some platforms only support one mode)
process-mode=1

# Toggle annotation types
display-text=1
display-bbox=0
display-mask=1

# Note: font size/color are controlled per-object via NvDsObjectMeta.text_params.
# Adjusted in code (deepstream_video_pipeline.py) for larger, readable labels.
