# Phase 2 Review — 2026-06-07

- Process supervisor with SIGINT process-group stop, log ring buffer, `/api/status` tail
- Built-in presets for canonical + experimental infer variants
- Restart API (`POST /api/restart`)