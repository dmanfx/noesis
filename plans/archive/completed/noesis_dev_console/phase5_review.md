# Phase 5 Review — 2026-06-07

- `manifest_data.yaml` catalogs 120 `NOESIS_*` knobs from runtime + hooks
- Knobs tab with group filter and restart-required badges
- Launch env override textarea (`KEY=value` per line)