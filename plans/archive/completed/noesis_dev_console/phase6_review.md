# Phase 6 Review — 2026-06-07

- Runtime proxy: REST depth refresh, WS command templates (trails, BEV, ping, clear_stats)
- Runtime tab WS client receives periodic `stats` broadcasts from child runtime
- v1 allowlist enforced in `runtime_proxy.py`